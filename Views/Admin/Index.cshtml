@using ShopWebApp
@{
    Layout = "_AdminLayout";
}
Użytkownicy sklepu:<br />
@{
    List<User> categoryList;
    using (var db = new ShopDatabase())
    {
        categoryList = (from c in db.Users select c).ToList<User>();
    }
    foreach(User c in categoryList) {
        @c.Name;@:<br />
    }
}